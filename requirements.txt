# Torch + CUDA come from the PyTorch Docker base image; do not pip-install CPU torch here.

ultralytics>=8.3.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.0
Pillow>=10.0.0
opencv-python-headless>=4.9.0.80
kaggle>=1.6.0
